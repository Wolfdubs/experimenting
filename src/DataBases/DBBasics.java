package DataBases;

public class DBBasics {
    /*
    Normalized DB = data segmented across tables, with other tables only able to access that data using foreign key references to the other table via JOIN
    Denormalized = data is repeated in tables, so redundant. Much more scalable as advantage of not needing expensive JOINs to reference wider data relationships
     */
}
