import java.util.Random;
import static utils.Utils.printArray;
import static Sorts.Sort.insertionSort;
import static Problems.Fibonacci.*;


public class Main {
    public static void main(String[] args) {


/*

        //create an array and make each element random
        int[] numArray = new int[20];
        Random rand = new Random();
        for (int i=0; i<numArray.length; i++) {
            numArray[i] = rand.nextInt(100);
        }
        System.out.println(("My unsorted random array = " ));
        printArray(numArray);
        insertionSort(numArray);
        System.out.println("\nMy sorted random array = ");
        printArray(numArray);

        //fibonnaci call
        System.out.println("\nThe recursive fibonacci number at the entered index is " + fibonacciRecursive(11));
        System.out.println("The memoized fibonacci number at the entered index is " + fibonacciMemoization(90));  //long limit at 92
        printFibonacci(20);
*/


    }
}
